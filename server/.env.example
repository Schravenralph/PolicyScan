# Server Environment Variables

# Embedding Configuration
# Enable async embedding generation during scraping (default: false)
# When enabled, documents are saved immediately and embeddings are generated in the background
EMBEDDING_ENABLED=false

# Embedding model to use (default: Xenova/all-MiniLM-L6-v2)
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2

# Hybrid Retrieval Configuration
# Enable hybrid retrieval in scan_known_sources workflow action (default: false)
# When enabled, combines keyword-based scraping with semantic vector search
# for improved document retrieval. Falls back to scraper-only if hybrid retrieval fails.
HYBRID_RETRIEVAL_ENABLED=false

# CSRF Protection
# Secret key for CSRF token generation (REQUIRED for production)
# Generate a secure random secret using:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# In development, defaults to an insecure value if not set
CSRF_SECRET=change-me-in-production-csrf-secret-key

# Knowledge Graph Backend Configuration
# Select the knowledge graph backend: 'graphdb' (default) or 'neo4j'
# Note: Hierarchical structure features require Neo4j backend
KG_BACKEND=graphdb
