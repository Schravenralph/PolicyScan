/**
 * Upload Progress Component
 *
 * Displays upload progress with percentage and progress bar.
 */
interface UploadProgressProps {
    progress: number;
    isUploading: boolean;
}
export declare function UploadProgress({ progress, isUploading }: UploadProgressProps): import("react/jsx-runtime").JSX.Element | null;
export {};
