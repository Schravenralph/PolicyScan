/**
 * Shared types for API services
 * Re-export pagination types from shared location
 */

export type { PaginationMetadata, PaginationMetadataUnion, AlternativePaginationMetadata } from '../../types/pagination';

